imporht pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import classification_report, accuracy_score
import re

# 1. Load Dataset
import kagglehub
from kagglehub import KaggleDatasetAdapter

# Set the path to the file you'd like to load
file_path = "/content/drive/MyDrive/IMDB Dataset.csv"
df = pd.read_csv(file_path)


print("First 5 records:", df.head())

# 2. Simple Cleaning Function
def clean_text(text):
    text = text.lower()
    text = re.sub(r'<br />', ' ', text) # Remove HTML
    text = re.sub(r'[^a-zA-Z]', ' ', text) # Remove punctuation/numbers
    return text

df['review'] = df['review'].apply(clean_text)

# 3. Split Data (80% Train, 20% Test)
X_train, X_test, y_train, y_test = train_test_split(
    df['review'], df['sentiment'], test_size=0.2, random_state=42
)

# 4. Vectorization (TF-IDF)
tfidf = TfidfVectorizer(max_features=5000)
X_train_tfidf = tfidf.fit_transform(X_train)
X_test_tfidf = tfidf.transform(X_test)

# 5. Train Model
model = LogisticRegression()
model.fit(X_train_tfidf, y_train)

# 6. Evaluate
print("\n\n\n")
predictions = model.predict(X_test_tfidf)
print(f"Accuracy: {accuracy_score(y_test, predictions)}")
print(classification_report(y_test, predictions))
print('\n\n\n')


# 7. Prediction function to evaluate some natural example

def predict_review(review):
  for r in review:
    print(r, model.predict(tfidf.transform([r])))

print("\nPrediction on pure test set\n")
reviews= [
"The movie was engaging with good performances, though a bit predictable at times. Overall, a decent watch.",

"I really enjoyed this film, with great acting and a compelling story. Highly recommended.",

"The plot was confusing and inconsistent, making it hard to stay interested. Disappointing overall.",

"A fun and lighthearted movie with good humor and chemistry. Worth watching for entertainment."
]

predict_review(reviews)
